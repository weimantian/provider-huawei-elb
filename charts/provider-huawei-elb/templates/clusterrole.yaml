{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "provider-huawei-elb.fullname" . }}
  labels:
    {{- include "provider-huawei-elb.labels" . | nindent 4 }}
# Rules are generated by controller-gen from kubebuilder RBAC markers.
# Run `make generate` to update.
{{ .Files.Get "generated/rbac-rules.yaml" | nindent 0 }}
{{- end }}
