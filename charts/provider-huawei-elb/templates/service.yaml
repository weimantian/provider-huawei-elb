apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-huawei-elb.fullname" . }}
  labels:
    {{- include "provider-huawei-elb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-huawei-elb.selectorLabels" . | nindent 4 }}
